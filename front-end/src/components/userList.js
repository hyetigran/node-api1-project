import React from "react";
import UserCard from "./userCard";

const userList = props => {
  return (
    <div>
      <div>user List</div>
      <UserCard />
    </div>
  );
};

export default userList;
